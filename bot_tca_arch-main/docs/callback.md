# 🔗 Пакет `callback`

## 📋 Оглавление
1. [Описание пакета](#описание-пакета)
2. [Внутренняя архитектура](#внутренняя-архитектура)
3. [Чек-лист добавления элементов](#чек-лист-добавления-элементов)

---

## Описание пакета

### 🎯 Назначение
Пакет `callback` содержит **классы колбеков для Telegram**, организованные по **тегам** (функциональным областям). Колбеки используются в Answer-компонентах из `node/` для построения inline-клавиатур.

### 🏷️ Система тегов
Колбеки группируются по **тегам** - логическим блокам функциональности.

**Примеры тегов:**
- `onboarding` - процесс знакомства с ботом
- `payment` - обработка платежей
- `interview` - проведение интервью

### 📁 Что содержит
- **Модули по тегам:** Каждый тег = отдельный модуль
- **Классы колбеков:** Каждый виджет = отдельный класс

---

## Внутренняя архитектура

### 📂 Структура пакета

```
callback/
├── __init__.py           # Базовые утилиты
├── {tag_name}.py         # Модуль для конкретного тега
└── {tag_name}.py         # Модуль для другого тега
```

**Пример структуры:**
```
callback/
├── __init__.py           # Базовые утилиты
├── onboarding.py         # Колбеки для онбординга
├── payment.py            # Колбеки для платежей
└── interview.py          # Колбеки для интервью
```

### 📱 Пример создания колбеков

**Допустим, у нас есть тег `onboarding` и мы хотим создать 2 виджета:**
1. **SetCompanyName** - установка названия компании
2. **SetCompanyDescription** - установка описания компании

**Под каждый виджет заводится отдельный класс колбека.**

Тогда создаем модуль `onboarding.py`:

```python
"""
Колбеки для процесса онбординга пользователей.

Содержит колбеки для:
- SetCompanyNameCallback: Установки названия компании
- SetCompanyDescriptionCallback: Установки описания компании
"""

from aiogram.filters.callback_data import CallbackData

class SetCompanyNameCallback(CallbackData, prefix="onboarding_set_company_name"):
    """
    Колбек для установки названия компании.
    
    Описание: Позволяет пользователю установить название своей компании
    в процессе онбординга.
    
    Поля:
    - company_name: название компании (строка) - до 100 символов
    """
    company_name: str

class SetCompanyDescriptionCallback(CallbackData, prefix="onboarding_set_company_description"):
    """
    Колбек для установки описания компании.
    
    Описание: Позволяет пользователю добавить описание деятельности
    своей компании или пропустить этот шаг.
    
    Поля:
    - description: описание компании (строка) - до 500 символов, может быть пустым
    - skip: пропустить ввод описания (bool) - True если пропускаем
    """
    description: str = ""
    skip: bool = False
```

### 📝 Правила документации колбека

Каждый класс колбека должен содержать:

1. **Описание назначения** - для чего нужен этот колбек
2. **Описание каждого поля** - что означает поле и какие значения принимает

**Шаблон документации:**
```python
class SomeCallback(CallbackData, prefix="some_action"):
    """
    Колбек для {назначение}.
    
    Описание: {подробное описание что делает колбек}
    
    Поля:
    - field1: {описание поля} - {возможные значения/тип}
    - field2: {описание поля} - {возможные значения/тип}
    """
    field1: str
    field2: int
```

---

## Чек-лист добавления колбека

### ✅ Добавление нового колбека

- [ ] **Проверить существование модуля** - есть ли файл `callback/{tag_name}.py`
- [ ] **Если модуля нет** - создать новый модуль `{tag_name}.py`
- [ ] **Если модуль есть** - использовать существующий
- [ ] **Определить название виджета** - например `SetCompanyName`
- [ ] **Создать класс** по шаблону `{Widget}Callback`
- [ ] **Выбрать prefix** по шаблону `{tag}_{widget}`
- [ ] **Определить поля** и их типы
- [ ] **В документации есть описание виджета** - что делает колбек
- [ ] **В документации есть описание полей** - что означает каждое поле
- [ ] **Обновить документацию модуля** - удалить или добавить инфомрацию о колбеке

```python
# Шаблон для нового колбека
class {Widget}Callback(CallbackData, prefix="{tag}_{widget}"):
    """
    Колбек для {назначение виджета}.
    
    Описание: {подробное описание что делает этот виджет}
    
    Поля:
    - field1: {описание поля} - {тип и возможные значения}
    - field2: {описание поля} - {тип и возможные значения}
    """
    field1: str
    field2: int
```
