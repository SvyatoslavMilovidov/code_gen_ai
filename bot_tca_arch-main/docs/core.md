# 📦 Пакет `core`

## 📋 Оглавление
1. [Описание пакета](#описание-пакета)
2. [Внутренняя архитектура](#внутренняя-архитектура)
3. [Чек-лист добавления элементов](#чек-лист-добавления-элементов)

---

## Описание пакета

### 🎯 Назначение
Пакет `core` содержит **основные настройки, конфигурации и словари**, необходимые для работы всего бота.

### 📁 Что содержит
- **Конфигурация:** настройки бота, API ключи
- **Словари:** тексты, сообщения, константы
- **Промпты:** шаблоны для работы с AI
- **Загрузчики:** инициализация компонентов

---

## Внутренняя архитектура

### 📂 Структура пакета

```
core/
├── __init__.py           # Экспорт основных компонентов
├── config.py             # Основная конфигурация
├── vocab.py              # Словари и тексты
├── prompts.py            # Промпты для AI
├── loader.py             # Загрузчики и инициализация
└── gpt_style.py          # Стили для GPT запросов
```

### 🔧 Пример создания конфигурации: config.py

```python
"""
Основная конфигурация бота.
Содержит настройки подключения к API и параметры работы.
"""

import os

class Config:
    """Основной класс конфигурации."""
    
    # Telegram Bot
    BOT_TOKEN = os.getenv("BOT_TOKEN")
    
    # Database
    DB_HOST = os.getenv("DB_HOST", "localhost")
    DB_PORT = int(os.getenv("DB_PORT", "5432"))
    DB_NAME = os.getenv("DB_NAME", "hr_bot")
    
    # OpenAI
    OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")

config = Config()
```

### 📖 Пример создания словарей: vocab.py

```python
"""
Словари и тексты для пользовательского интерфейса.
Организованы по функциональным областям.
"""

MESSAGES = {
    'welcome': {
        'ru': '👋 Добро пожаловать в HR Bot!',
        'en': '👋 Welcome to HR Bot!'
    },
    'error_generic': {
        'ru': '❌ Произошла ошибка. Попробуйте позже.',
        'en': '❌ An error occurred. Please try again later.'
    }
}

BUTTONS = {
    'continue': {
        'ru': '➡️ Продолжить',
        'en': '➡️ Continue'
    },
    'cancel': {
        'ru': '❌ Отмена', 
        'en': '❌ Cancel'
    }
}
```

---

## Чек-лист добавления элементов

### ✅ Добавление новой конфигурации

- [ ] **Определить назначение** настройки
- [ ] **Добавить в Config** класс с типизацией
- [ ] **Добавить переменную окружения** в example.env
- [ ] **Написать комментарий** к настройке

### ✅ Добавление текстов в vocab.py

- [ ] **Определить категорию** текста
- [ ] **Добавить в соответствующий словарь**
- [ ] **Использовать описательное имя** ключа
- [ ] **Добавить перевод текста** на используемых языках

### ✅ Добавление промпта

- [ ] **Определить назначение** промпта
- [ ] **Написать четкий промпт** с инструкциями
- [ ] **Добавить параметры** для подстановки
- [ ] **Указать формат ответа** от AI
