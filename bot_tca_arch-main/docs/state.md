# 🔄 Пакет `state`

## 📋 Оглавление
1. [Описание пакета](#описание-пакета)
2. [Внутренняя архитектура](#внутренняя-архитектура)
3. [Чек-лист добавления элементов](#чек-лист-добавления-элементов)

---

## Описание пакета

### 🎯 Назначение
Пакет `state` содержит **описания состояний FSM**, организованные по **тегам** в соответствии с функциональными областями. Каждое состояние представляет определенный этап диалога пользователя с ботом. Состояния используются в Trigger-компонентах (для фильтрации) и Answer-компонентах (для установки) из `node/`.

### 🏷️ Система тегов
Состояния группируются по **тегам** - логическим блокам.

**Примеры тегов:**
- `onboarding` - состояния процесса знакомства
- `payment` - состояния обработки платежей  
- `interview` - состояния проведения интервью

### 📁 Что содержит
- **Модули по тегам:** Каждый тег = отдельный модуль
- **Переменные состояний:** Каждое состояние = константа

---

## Внутренняя архитектура

### 📂 Структура пакета

```
state/
├── __init__.py           # Экспорт состояний
├── {tag_name}.py         # Модуль для конкретного тега
└── {tag_name}.py         # Модуль для другого тега
```

**Пример структуры:**
```
state/
├── __init__.py           # Экспорт состояний
├── onboarding.py         # Состояния онбординга
├── payment.py            # Состояния платежей
└── interview.py          # Состояния интервью
```

### 📱 Пример создания состояний

**Допустим, у нас есть тег `onboarding` и нам нужны состояния для:**
1. **Ожидания названия компании** 
2. **Ожидания описания компании**
3. **Завершения онбординга**

Тогда создаем модуль `onboarding.py`:

```python
"""
Состояния для процесса онбординга пользователей.

Содержит состояния для:
- waiting_for_company_name: Ожидания ввода названия компании
- waiting_for_company_description: Ожидания ввода описания компании  
- onboarding_completed: Завершения процесса онбординга
"""

from aiogram.fsm.state import State

waiting_for_company_name = State()
"""
Ожидание ввода названия компании.

Назначение: Состояние устанавливается когда бот ожидает от пользователя 
ввод названия его компании в процессе онбординга.

Используется в виджетах:
TRIGGER: SetCompanyNameCallback, EditCompanyNameCallback
ANSWER: StartOnboardingCallback, RetryCompanyNameCallback
"""

waiting_for_company_description = State()
"""
Ожидание ввода описания компании.

Назначение: Состояние устанавливается когда пользователь должен ввести
описание деятельности своей компании или пропустить этот шаг.

Используется в виджетах:
TRIGGER: SetCompanyDescriptionCallback, SkipDescriptionCallback
ANSWER: SetCompanyNameCallback, EditCompanyDescriptionCallback
"""

onboarding_completed = State()
"""
Завершение процесса онбординга.

Назначение: Состояние устанавливается когда пользователь успешно 
завершил процесс онбординга и готов к работе с основным функционалом.

Используется в виджетах:
TRIGGER: 
ANSWER: SetCompanyDescriptionCallback, SkipDescriptionCallback, CompleteOnboardingCallback
"""
```

### 📝 Правила документации состояния

Каждое состояние должно содержать:

1. **Назначение** - когда и зачем используется это состояние
2. **Используется в виджетах** - список колбеков по группам TRIGGER и ANSWER

**Шаблон документации:**
```python
some_state = State()
"""
{Краткое описание состояния}.

Назначение: {подробное описание когда устанавливается состояние}

Используется в виджетах:
TRIGGER: {колбеки которые слушают это состояние}
ANSWER: {колбеки которые переводят в это состояние}
"""
```

---

## Чек-лист добавления состояния

### ✅ Добавление нового состояния

- [ ] **Проверить существование модуля** - есть ли файл `state/{tag_name}.py`
- [ ] **Если модуля нет** - создать новый модуль `{tag_name}.py`
- [ ] **Если модуль есть** - использовать существующий
- [ ] **Определить назначение состояния** - когда оно устанавливается
- [ ] **Создать переменную** с описательным именем
- [ ] **В документации есть назначение** - когда устанавливается состояние
- [ ] **В документации есть TRIGGER виджеты** - какие колбеки слушают это состояние
- [ ] **В документации есть ANSWER виджеты** - какие колбеки переводят в это состояние
- [ ] **Обновить документацию модуля** - добавить информацию о новом состоянии

```python
# Шаблон для нового состояния
{descriptive_name} = State()
"""
{Краткое описание состояния}.

Назначение: {подробное описание когда устанавливается это состояние}

Используется в виджетах:
TRIGGER: {колбеки которые слушают это состояние}
ANSWER: {колбеки которые переводят в это состояние}
"""
```
